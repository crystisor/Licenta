# Expo only reads env vars prefixed with EXPO_PUBLIC_ in the client bundle.
# For Android/iOS devices, use your machine's LAN IP instead of localhost.
EXPO_PUBLIC_API_BASE_URL="http://127.0.0.1:8001"
